from setuptools import setup, find_packages

setup(
    name='trend_analysis_tools',
    version='1.0.0',
    packages=find_packages(),
    install_requires=[],
    description='A collection of tools to analyse markets.',
)
